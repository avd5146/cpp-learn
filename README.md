# cpp-learn
Coding practice

# Build instructions
- Builds with windows configurations in VS code with necessary compilers.
- pure cmake option works too. 
In root:
```
mkdir build_cmake
cd build_cmake
cmake ..
make
```